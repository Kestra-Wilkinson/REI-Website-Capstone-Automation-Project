class BasePage { constructor(driver) { this.driver = driver; } async visit(url) { await this.driver.get(url); } async waitUntilVisible(locator, timeout = 10000) { const { until } = require('selenium-webdriver'); return this.driver.wait(until.elementLocated(locator), timeout); } async click(locator) { const element = await this.waitUntilVisible(locator); await element.click(); } async getText(locator) { const element = await this.waitUntilVisible(locator); return element.getText(); } } module.exports = BasePage;