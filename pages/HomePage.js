const { By } = require('selenium-webdriver'); const BasePage = require('./BasePage'); class HomePage extends BasePage { constructor(driver) { super(driver); } async hoverMainMenu(name) { const actions = this.driver.actions({ async: true }); const locator = By.css(`a[data-testid="gnav-header-link-${name}"]`); const menu = await this.waitUntilVisible(locator); await actions.move({ origin: menu }).perform(); } async clickSubMenu(name) { const locator = By.xpath(`//a[contains(text(), '${name}')]`); await this.click(locator); } } module.exports = HomePage;