const { By, Key } = require('selenium-webdriver'); const BasePage = require('./BasePage'); class SearchPage extends BasePage { constructor(driver) { super(driver); } async search(term) { const input = await this.waitUntilVisible(By.id('search-input')); await input.clear(); await input.sendKeys(term, Key.RETURN); } async getProductResults() { const items = await this.driver.findElements(By.css('[data-testid="product-card"]')); return items.length; } } module.exports = SearchPage;